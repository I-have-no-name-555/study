package gof.behavioral;

/**
 * @author :Xuan
 * @date :Create in 2020/12/20 17:54
 * @description 迭代器
 */
public class Iterator {
    /*
        jdk中的迭代器模式：
            集合下的各种类，如ArrayList、LinkedList等
        迭代器模式：
            不同的类对于元素的组织形式可能不同，但只需给出一个迭代器就可以统一接口而不必暴露细节
            每个聚合对象都需要一个迭代器，会有多个迭代器类
            将每个结构的迭代器写成内部类便于管理

     */
}
